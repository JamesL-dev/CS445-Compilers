#DRBC All production rules Full test
// /\ precomList
// \/ program

// <CONSTANT> ::= NUMCONST | CHARCONST | STRINGCONST | BOOLCONST
// int x:5, y:1337;
char booger[15]:"picking";

main()
{
}

// <parmId> | <parmLisT> | <parms> | <parmIdList> | <funDecl>
int bar(int x, b[]; bool y, d; int e; char f)
{
  // <assignop>
  x = 1;
  x += 1;
  x -= 1;
  x *= 1;
  x /= 1;

  // <simpleExp>
  y = y or y;

  // <andExp>
  y = y and y;

  // <unaryRelExp>
  y = not y;

  // <relExp> | <relop>
  y = x == x;
  y = x != x;
  y = x <= x;
  y = x < x;
  y = x >= x;
  y = x > x;
  y = x < x;

  // <minmaxExp>
  x = x :>: x;
  x = x :<: x;

  // <expstmt>
  x;

  // <compoundstmt>
  {x++; b[1]--;};

  // <call>
  main();

  // <call> | <arglist>
  bar(1, b, true, false, 123, 'f');

  // <unaryexp> | <unaryop>
  *b;
  ?x;
  -x;

  // <mulExp> | <mulop>
  x*x;
  x/x;
  x%x;

  // <sumop> | <sumexp>
  x+x;
  x-x;

  // <matched> | <unmatched>
  while true do
  {
    // <scopedVarDeclList>
    static int variable;
    if 1 < 2 then
    {
      break;
    }
    else
    {
      break;
    }
  }
  
  // <iterrange>
  for i = 1 to 10 by 3 do 
  {

  }
  
  // <returnstmt>
  return 3;

}

